name: get_internationalization
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

publish_to: 'none'

environment:
  sdk: ^3.2.3

# Add regular dependencies here.
dependencies:
  source_gen: ^1.5.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
  build_runner: ^2.0.0
  analyzer: ^6.4.1
